apiVersion: policy.mcm.ibm.com/v1alpha1
kind: Policy
metadata:
  name: policy-verify-[TIME]
  namespace: default
  annotations:
    policy.mcm.ibm.com/standards: 
    policy.mcm.ibm.com/categories: 
    policy.mcm.ibm.com/controls: 
spec:
  complianceType: musthave
  remediationAction: inform
  disabled: false
  namespaces:
    exclude: ["kube-*"]
    include: ["default"]
  policy-templates:
    - objectDefinition:
      apiVersion: policy.mcm.ibm.com/v1alpha1
      kind: Policy
      metadata:
        name: policy-pod
        namespace: default
        annotations:
          policy.mcm.ibm.com/standards: NIST-CSF
          policy.mcm.ibm.com/categories: PR.PT Protective Technology
          policy.mcm.ibm.com/controls: PR.PT-3 Least Functionality
      spec:
        complianceType: musthave
        remediationAction: inform
        disabled: false
        namespaces:
          exclude: ["kube-*"]
          include: ["default"]
---
apiVersion: mcm.ibm.com/v1alpha1
kind: PlacementBinding
metadata:
  name: binding-policy-verify-[TIME]
  namespace: default
placementRef:
  name: placement-policy-verify-[TIME]
  kind: PlacementRule
  apiGroup: apps.open-cluster-management.io
subjects:
- name: policy-verify-[TIME]
  kind: Policy
  apiGroup: policy.mcm.ibm.com
---
apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: placement-policy-verify-[TIME]
  namespace: default
spec:
  clusterConditions:
  - type: OK
  clusterSelector:
    matchExpressions:
      []
